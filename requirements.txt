# Serverless bundle: the /cron/ingest route runs the full ingest cycle
# in-function, so httpx/bs4 ride along (onlinejobs parses via stdlib html.parser).
# libsql is needed when GIGGREGATOR_TURSO_DATABASE_URL is set (ADR-0011 remote backend).
fastapi>=0.115
httpx>=0.27
beautifulsoup4>=4.12
libsql>=0.1.11
